# ── Port ─────────────────────────────────────────────────────────────────────
# Port the web UI listens on (dev and production).
# Default: 3000
PORT=3000

# ── Kubeconfig ────────────────────────────────────────────────────────────────
# Option 1 — explicit path (takes priority)
# KUBE_CONFIG_PATH=/home/alice/.kube/my-cluster.yaml

# Option 2 — standard kubectl env var (respected by loadFromDefault)
# KUBECONFIG=/home/alice/.kube/config

# If neither is set, defaults to ~/.kube/config
